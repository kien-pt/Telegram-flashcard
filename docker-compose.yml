version: '3.8'

services:
  flashcard:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
